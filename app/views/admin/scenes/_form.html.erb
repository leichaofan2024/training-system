<%= simple_form_for [:admin,@scene] do |f| %>
  <%=f.input :title, label: "场景标题" %>
  <%= f.input :description, label: "操作手册"%>
  <%=f.submit "创建", data: {disable_with: "提交中..."}, class: "btn btn-info"%>
<% end %>
